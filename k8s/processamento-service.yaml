apiVersion: v1
kind: Service
metadata:
  name: processamento-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: processamento-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 30085
